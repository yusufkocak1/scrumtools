package com.scrumtools.entity.enums;

/**
 * Desteklenen CI/CD sağlayıcıları. İlk sürümde yalnız Jenkins implemente edilir;
 * enum ve Ci* soyutlaması GitLab CI / GitHub Actions'ın sonradan eklenebilmesi için vardır.
 */
public enum CiProvider {
    JENKINS
}
