package com.scrumtools.entity.enums;

/**
 * Dinamik özel alan tipleri.
 */
public enum CustomFieldType {
    TEXT,
    NUMBER,
    DATE,
    SELECT,
    MULTI_SELECT,
    USER,
    URL,
    CHECKBOX
}

